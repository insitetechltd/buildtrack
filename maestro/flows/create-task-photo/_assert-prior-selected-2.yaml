# Assert library shows exactly 2 prior selected (ledger invariant).
appId: com.buildtrack.app.local
---
- extendedWaitUntil:
    visible:
      id: "in-app-library__screen"
    timeout: 8000
- assertVisible: ".*2 selected.*"
- takeScreenshot: "ct-photo-ledger-2-selected"
