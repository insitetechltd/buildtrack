---
name: solo-dev-harness
description: >-
  Portable ultra-quality multi-agent cycle: Scout → Spec → Gate A → Test
  contract → Build → Prove → Quality Judge loop (PATCH/REWORK/REDESIGN/SHIP).
  Use at session start and whenever encoding process changes. Dual-write SOP.md
  + templates/ + the consuming repo. Project-only law stays in the project overlay.
---

# Solo Dev Harness

`SOP.md` is the whole-cycle SoT (roles, tracks S/M/U, scorecard, verdicts, claim ledger).

Copy-paste artifacts live in `templates/` (scorecard, tracks, claim ledger, Judge handoff, shared brief).

When this personal skill is missing, use the git-tracked copy: `docs/superpowers/templates/solo-dev-harness/` in the consuming repo.
