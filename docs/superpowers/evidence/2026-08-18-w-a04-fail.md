# W-A04 headed fail (2026-08-18)

**Journey:** Activity row → Task Detail (`dashboard-screen__activity_*` or critical-task row → `task-detail__header_title`).

**Result:** FAIL — no Activity row to tap. Did not reach Task Detail.

**Sim:** iPhone 17 Pro `702680D5-A92E-4C56-BE55-731D424FE63A` (headed, no `--headless`). John Manager, Project A.

**What the PNG shows** (`2026-08-18-w-a04-empty-activity.png`, Maestro step after no-clear relaunch at 22:29):

- Activity tab, project summary present (W-A02 still true): *Project A - Commercial Building* / Tuesday · Aug 18 · Day 16 · 28°C
- Critical tasks: *No critical tasks flagged for this week yet.*
- Queue tiles all **0**
- Recent Activity: *No recent activity for the current project yet.*
- No Drafts In Progress

**Contrast:** Human screenshot at 22:25 on the same sim showed critical-task rows (Electrical Wiring Phase 1, Maestro CT Photo…). `launchApp` without `clearState` still restarted JS and the dashboard came back empty.

**Maestro:** `P-B-W-A04.yaml` rc=1. Secondary: XCTest `Device became unreachable during viewHierarchy` while looking for a critical-task id. Not the product reason — the row was already gone.

**Not attempted:** sandbox seed / cache-delay debug (parked).

Follow-on: W-A05 fail (no drafts). W-A06/W-A07 skipped (same blocker). W-A08 never reached camera — XCTest died after login while waiting for `dashboard-screen__root`. PNG at that moment showed critical rows again (Electrical Wiring + Maestro CT Photo).

Attach-only retry (`P-B-W-A04-attach.yaml`): FAIL `Element not found: Electrical Wiring Phase 1`. PNG `2026-08-18-w-a04-attach-create-form.png` is **Create New Task** with keyboard up, not Activity. Stopped (no further selector/back recovery).
