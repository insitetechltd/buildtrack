/**
 * @deprecated Prefer ModernScreenHeader. Kept as a stable import alias with the
 * same ProjectPicker navigation path (Activity nested route).
 */
export { default } from "./ModernScreenHeader";
