# Assert library shows exactly 3 prior selected (ledger invariant).
appId: com.buildtrack.app.local
---
- extendedWaitUntil:
    visible:
      id: "in-app-library__screen"
    timeout: 8000
- assertVisible: ".*3 selected.*"
- takeScreenshot: "ct-photo-ledger-3-selected"
