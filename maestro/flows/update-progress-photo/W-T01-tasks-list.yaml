# W-T01: Tasks tab shows the task list.
appId: com.buildtrack.app.local
tags:
  - rc-headed-d01
---
- runFlow: ../create-task-photo/_boot.yaml
- runFlow: ../create-task-photo/_ensure-project.yaml
- runFlow: ../_dismiss-blocking-overlays.yaml

- tapOn:
    id: "root-tab__tasks_pressable"
- extendedWaitUntil:
    visible:
      id: "tasks-screen__root"
    timeout: 25000
- extendedWaitUntil:
    visible:
      id: "tasks-screen__search_section"
    timeout: 15000
- extendedWaitUntil:
    visible:
      id: "tasks-screen__task_list"
    timeout: 25000
- assertVisible:
    id: "tasks-screen__filters_button"
- takeScreenshot: "rc-D-T01-tasks-list"
