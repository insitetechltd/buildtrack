# S-UX-01N Close Report (2026-08-08)

## Summary

Closed **S-UX-01N** after Human GO: `you have GO for S-UX-01N containers live apply`.

## Decisions

| ID | Choice |
|----|--------|
| N1 | `project_containers` (project-scoped; mirrors `project_locations`) |
| N2 | Production session pooler `:5432` |
| N3 | Keep task `container_id` / `sub_container_id` as **text** (03b D1); store UUID strings |

## Live apply

1. Forward `20260808110000_sux01n_project_containers.sql` — CREATE TABLE + indexes succeeded; helper-based policies failed (`user_has_project_access` **absent** on live, same as 02a interim note).
2. Fixup `20260808110002_sux01n_project_containers_rls_live.sql` — assignment-based RLS mirrored from `project_locations` + restrictive `anon_block_all`. Forward artefact updated to match.
3. Verify: table present (7 cols), 5 policies, anon SELECT → `insufficient_privilege` OK.

## App (already in tree)

- Progressive disclosure Create UI (hidden until expand / catalogue rows)
- Store `fetchProjectContainers` / `ensureProjectContainer` (degrades if table missing)
- Persist `containerId` / `subContainerId` on create/update

## Residual

- UUID FK upgrade (D2) still deferred
- Task Detail area labels still primarily via Edit/Create picker (no full detail catalogue UI)
- Scrub `~/.pgpass` after apply (session hygiene)
