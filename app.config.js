/**
 * Keep iOS bundle ID aligned with App Store Connect app 6754898737
 * (`com.buildtrack.app.local`). There is no `com.buildtrack.app` identifier
 * on the team; registering one is a new-app change, not an update.
 */
module.exports = ({ config }) => config;
