# M-DATA-04 close — Realtime subscription / reconnect storm

**Closed:** 2026-08-27  
**Milestone:** `WS-DATA / M-DATA-04` (ROADMAP Order 14.95)  
**Status:** Closed — RC blocker cleared

---

## Evidence

### Prior

- Investigation: `docs/superpowers/analysis/2026-08-20-db-spike-realtime-churn.md`
- Gate SQL: `docs/superpowers/sql/2026-08-20-m-data-04-realtime-gate.md`
- App hygiene: `RealtimeSyncManager` / `realtimeReconnect` dampening (2026-08-20+)
- **2026-08-21:** human 30s idle — subscription inserts flat (PASS)

### Formal close sample (2026-08-27)

Owner ran gate queries A + B on production after `pg_stat_statements_reset` (idle / controlled clients; no Maestro stampede).

**B — Realtime churn (excerpt):**

| query_head (abbrev) | calls |
|---|---|
| `list_changes` (`wal->>`) | 168 |
| `with sub_tables` (publication) | 28 |
| `delete from realtime.subscription` | 1 |

No flood of `INSERT realtime.subscription` (thousands).

**A — Top time (excerpt):**

| query_head (abbrev) | calls | total_ms | mean_ms |
|---|---|---|---|
| `list_changes` (`wal->>`) | 218 | **1240.7** (~1.2 s) | 5.691 |
| tasks REST | 4 | 66.3 | 16.6 |
| task_activities REST | 4 | 36.8 | 9.2 |
| users REST | 5 | 24.4 | 4.9 |
| `with sub_tables` | 28 | 15.4 | 0.55 |

Pre-fix failure mode was ~**486 s / ~81k** `list_changes` and thousands of subscription inserts. Close sample is orders of magnitude quieter; REST fetches remain single-digit.

**Waived as optional (owner GO 2026-08-27):** re-proof of airplane-mode 30s reconnect + full 10‑minute idle seq_scan window — accepted on dampening + idle PASS + 2026-08-27 SQL sample.

---

## Residual

- Maestro `clearState` can still amplify subscribe cycles in CI — keep Maestro/CI on DEV; avoid multi-sim authenticated prod.
- Unfiltered channel design may still warrant future filters; not reopening this milestone unless churn returns at production scale.
