# W-A08 headed fail (2026-08-18)

**Journey:** Camera FAB from Activity → Create Task form (`root-tab__camera_button` → `create-task__continuous_form`).

**Result:** FAIL — never tapped the FAB.

**Maestro:** `P-B-W-A08.yaml` rc=1. After Sign In, XCTest `Device became unreachable during viewHierarchy` while waiting for `dashboard-screen__root`.

**PNG** `2026-08-18-w-a08-activity-before-driver-drop.png`: Activity is populated (Electrical Wiring + Maestro CT Photo rows) and the red camera FAB is visible. Product likely would pass if the driver held. Did not retry.
